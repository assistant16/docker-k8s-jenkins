apiVersion: v1
kind: Service
metadata:
  name: test-app-service
  namespace : jenkins
spec:
  selector: 
    app: test-app
  type: NodePort  
  ports:
    - name: httpport-test
      port: 6379 
      targetPort: 6379 
      nodePort: 6379 
